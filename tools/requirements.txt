# Build-time dependencies for the technique importer.
#
# These are NOT runtime dependencies — they're only needed when you run
# ./build.sh to refresh the technique library from upstream.
#
# Install with:  pip install -r tools/requirements.txt
#                pip install -r tools/requirements.txt --break-system-packages   (Ubuntu 24.04+)

pyyaml>=6.0
